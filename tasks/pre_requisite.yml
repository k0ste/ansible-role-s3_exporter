---
- name: "s3_exporter | Assert that ansible facts gathered successfully"
  ansible.builtin.assert:
    that:
      - "hostvars[inventory_hostname]['ansible_facts']['service_mgr'] is
        defined"
    fail_msg: "'ansible_facts' not found. Please enable facts gathering"
    quiet: "true"
- name: "s3_exporter | Assert that system manager is systemd"
  ansible.builtin.assert:
    that:
      - "hostvars[inventory_hostname]['ansible_facts']['service_mgr'] ==
        'systemd'"
    fail_msg: "System manager is not systemd"
    quiet: "true"
- name: "s3_exporter | Add the OS specific variables"
  ansible.builtin.include_vars:
    file: "{{ hostvars[inventory_hostname]['ansible_facts']['os_family'] +
      '.yml' }}"
- name: "s3_exporter | Set facts about s3_exporter role"
  ansible.builtin.set_fact:
    s3_exporter_version: "{{ lookup('ansible.builtin.vars',
      's3_exporter') | community.general.json_query('[].version | [0]') }}"
    s3_exporter_settings: "{{ lookup('ansible.builtin.vars',
      's3_exporter') | community.general.json_query('[].settings | [0]') }}"
    s3_exporter_package_state: "{{ lookup('ansible.builtin.vars',
      's3_exporter') |
      community.general.json_query('[].package_state | [0]') }}"
  when:
    - "lookup('ansible.builtin.vars', 's3_exporter', default='')
      not in ['', [], '[]', None]"
- name: "s3_exporter | Assert that s3_exporter package state in
    valid value"
  ansible.builtin.assert:
    that:
      - "lookup('ansible.builtin.vars', 's3_exporter_package_state') in
        ['present', 'latest']"
    fail_msg: "{{ 'package_state must be in `present` or `latest`,
      current is: ' + lookup('ansible.builtin.vars',
      's3_exporter_package_state') }}"
    quiet: "true"
  when:
    - "lookup('ansible.builtin.vars', 's3_exporter_package_state',
      default='') not in ['', [], '[]', None, 'present', 'latest']"
- name: "s3_exporter | Set package state to 'present' cause value is not
    defined"
  ansible.builtin.set_fact:
    s3_exporter_package_state: "present"
  when:
    - "lookup('ansible.builtin.vars', 's3_exporter_package_state',
      default='') in ['', [], '[]', None]"
- name: "s3_exporter | Get last release version from Github"
  community.general.github_release:
    user: "ribbybibby"
    repo: "s3_exporter"
    action: "latest_release"
  run_once: true
  register: "s3_exporter_version_tag"
  delegate_to: "localhost"
  when:
    - "lookup('ansible.builtin.vars', 's3_exporter_version',
      default='') == 'latest'"
    - "lookup('ansible.builtin.vars', 's3_exporter', default='')
      not in ['', [], '[]', None]"
    - "lookup('ansible.builtin.vars', 's3_exporter') |
       community.general.json_query(lookup('ansible.builtin.vars',
      's3_exporter_install_package', default='')) == 'false'"
  vars:
    s3_exporter_install_package: "[] |
      map(&install_package || 'false', @) | [0]"
- name: "s3_exporter | Set facts about s3_exporter role"
  ansible.builtin.set_fact:
    s3_exporter_package_state: "present"
    s3_exporter_version: "{{ lookup('ansible.builtin.vars',
      's3_exporter_version_tag')['tag'][1:] }}"
  when:
    - "lookup('ansible.builtin.vars', 's3_exporter_version_tag',
      default='') not in ['', [], '[]', None]"
    - "not lookup('ansible.builtin.vars',
      's3_exporter_version_tag')['skipped']"
- name: "s3_exporter | Create s3_exporter catalog"
  ansible.builtin.file:
    path: "{{ lookup('ansible.builtin.vars', 's3_exporter_dest') }}"
    state: "directory"
    owner: "root"
    group: "root"
    mode: "0755"
  when:
    - "lookup('ansible.builtin.vars', 's3_exporter', default='')
      not in ['', [], '[]', None]"
    - "lookup('ansible.builtin.vars', 's3_exporter') |
       community.general.json_query(lookup('ansible.builtin.vars',
      's3_exporter_install_package', default='')) == 'false'"
  vars:
    s3_exporter_install_package: "[] |
      map(&install_package || 'false', @) | [0]"
